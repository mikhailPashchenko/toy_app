<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<strong><%= "#{@user.name}'s posts:" if @microposts.any? =%></strong>
<% @microposts.each do |post| %>
  <p><%= link_to "#{post.content}", post =%></p>
<% end %>
<br>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
